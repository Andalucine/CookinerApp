# Modelo de datos

Estado: **pendiente**. Se rellena con la primera migración (grupos, usuarios, recetas, ingredientes, vinos).

Regla: cada cambio en `backend/app/models/` actualiza este documento en el mismo commit.
