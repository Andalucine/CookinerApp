/** Placeholder: this section arrives in a later session (see the session plan in the bible). */
import { ComingSoon } from "../../components/ComingSoon.tsx";

export default function Section() {
  return <ComingSoon />;
}
